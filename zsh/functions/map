emulate -L zsh

typeset a ___=$1; shift

# if mapping expression EXPR does not contain $ then
# wrap it as ${aEXPR} 
if [[ $___ != *\$* ]] ; then
   echo here 
   ___="\${\${a}$___}"
   zvvv ___
fi

for a ; do
    print -- ${(e)___}
done

