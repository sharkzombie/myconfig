;; -*- mode: lisp -*-

(print "Starting .clisprc")

(load (make-pathname
       :directory
       (append (pathname-directory (user-homedir-pathname)) 
               '("myconfig" "dotfiles"))
       :name "lisp-init-1"
       :type "lisp"))


